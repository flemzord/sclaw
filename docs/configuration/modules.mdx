---
title: Module Configuration
description: "Configure channels, providers, memory backends, and tools"
icon: "puzzle-piece"
---

Modules are identified by their registration ID, which follows the pattern `<category>.<name>` (e.g., `channel.telegram`, `provider.openai_compatible`, `memory.sqlite`). Each key under `modules` must match a compiled module ID; unknown IDs cause a validation error.

## channel.telegram

Connects sclaw to Telegram as a messaging channel.

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `token` | string | — | Bot token in `<bot_id>:<hash>` format. **Required.** |
| `mode` | string | `"polling"` | Delivery mode: `"polling"` or `"webhook"`. |
| `polling_timeout` | int | `30` | Long-polling timeout in seconds (0–50). |
| `webhook_url` | string | — | Public URL for webhook mode. |
| `webhook_secret` | string | — | Secret token for webhook verification. |
| `allowed_updates` | list | `["message", "edited_message", "channel_post"]` | Telegram update types to receive. |
| `allow_users` | list | — | Telegram user IDs allowed to interact. Empty = all. |
| `allow_groups` | list | — | Telegram group IDs allowed. Empty = all. |
| `max_message_length` | int | `4096` | Maximum outbound message length (1–4096). |
| `stream_flush_interval` | duration | `1s` | Interval between streaming message edits (100ms–30s). |
| `api_url` | string | `"https://api.telegram.org"` | Telegram Bot API base URL. |

<Tabs>
  <Tab title="Polling (simple)">
    ```yaml
    modules:
      channel.telegram:
        token: "${TELEGRAM_BOT_TOKEN}"
        mode: "polling"
        allow_users: ["123456789"]
    ```
  </Tab>
  <Tab title="Webhook (production)">
    ```yaml
    modules:
      channel.telegram:
        token: "${TELEGRAM_BOT_TOKEN}"
        mode: "webhook"
        webhook_url: "https://bot.example.com/webhook"
        webhook_secret: "${WEBHOOK_SECRET}"
    ```
  </Tab>
</Tabs>

## provider.openai_compatible

Connects to any OpenAI-compatible API (OpenAI, Azure OpenAI, Anthropic via proxy, local models, etc.).

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `base_url` | string | — | API base URL (must be http or https). **Required.** |
| `api_key` | string | — | API key. One of `api_key` or `api_key_env` is **required**. |
| `api_key_env` | string | — | Environment variable name containing the API key. |
| `model` | string | — | Model identifier (e.g., `"gpt-4o"`). **Required.** |
| `context_window` | int | `4096` | Maximum context window size in tokens. |
| `max_tokens` | int | `0` | Maximum tokens to generate (0 = provider default). |
| `headers` | map | — | Extra HTTP headers sent with every request. |
| `timeout` | duration | `30s` | HTTP request timeout. |

```yaml
modules:
  provider.openai_compatible:
    base_url: "https://api.openai.com/v1"
    api_key: "${OPENAI_API_KEY}"
    model: "gpt-4o"
    context_window: 128000
    timeout: 30s
```

<Tip>
Use `api_key_env` instead of `api_key` if you prefer to reference the environment variable by name rather than expanding it inline. This can be useful when the same config is shared across environments.
</Tip>

## provider.openai_responses

Connects to OpenAI's Responses API via native WebSocket streaming. Lower latency than REST+SSE thanks to persistent connections.

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `api_key` | string | — | API key. One of `api_key` or `api_key_env` is **required**. |
| `api_key_env` | string | — | Environment variable name containing the API key. |
| `model` | string | — | Model identifier (e.g., `"gpt-4o"`). **Required.** |
| `context_window` | int | `128000` | Maximum context window size in tokens. |
| `max_output_tokens` | int | `0` | Maximum tokens to generate (0 = provider default). |
| `ws_endpoint` | string | `"wss://api.openai.com/v1/responses"` | WebSocket endpoint URL (must be ws or wss). |
| `headers` | map | — | Extra HTTP headers sent during WebSocket handshake. |
| `dial_timeout` | duration | `10s` | Timeout for establishing a WebSocket connection. |
| `conn_max_age` | duration | `55m` | Maximum connection lifetime before recycling. |

```yaml
modules:
  provider.openai_responses:
    api_key: "${OPENAI_API_KEY}"
    model: "gpt-4o"
    context_window: 128000
    max_output_tokens: 4096
```

<Tip>
Use `provider.openai_responses` for direct OpenAI access with lower latency. Use `provider.openai_compatible` for third-party APIs (Ollama, Azure, vLLM, etc.) that implement the Chat Completions interface.
</Tip>

## memory.sqlite

Provides SQLite-backed persistent conversation history and long-term memory.

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `path` | string | `{DataDir}/memory.db` | Database file path. |
| `wal` | bool | `true` | Enable WAL journal mode for concurrent reads. |
| `busy_timeout` | int | `5000` | Milliseconds to wait on a busy lock. Must be non-negative. |

<Tabs>
  <Tab title="Default (zero config)">
    ```yaml
    modules:
      memory.sqlite: {}
    ```
  </Tab>
  <Tab title="Custom path">
    ```yaml
    modules:
      memory.sqlite:
        path: "/var/lib/sclaw/memory.db"
        wal: true
        busy_timeout: 10000
    ```
  </Tab>
</Tabs>

<Note>
The SQLite module uses `modernc.org/sqlite` (pure Go, no CGO required). WAL mode enables concurrent reads while maintaining write serialization.
</Note>

## tool.shell

Configurable replacement for the built-in `exec` tool. When this module is active, it replaces the hardcoded exec tool with configurable timeouts, output limits, and approval policy.

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `timeout` | duration | `"30s"` | Default command timeout. |
| `max_timeout` | duration | `"10m"` | Maximum allowed timeout (caps per-command overrides). |
| `max_output_size` | int | `1048576` | Maximum stdout+stderr captured in bytes (1 MiB). |
| `default_policy` | string | `"allow"` | Default approval level: `"allow"`, `"ask"`, or `"deny"`. |

<Tabs>
  <Tab title="Default (zero config)">
    ```yaml
    modules:
      tool.shell: {}
    ```
  </Tab>
  <Tab title="Custom">
    ```yaml
    modules:
      tool.shell:
        timeout: "1m"
        max_timeout: "5m"
        max_output_size: 2097152
        default_policy: "ask"
    ```
  </Tab>
</Tabs>

<Note>
The tool name remains `exec` for backward compatibility with existing agent configurations that reference `tools: [exec]`.
</Note>

## tool.file_read

Configurable replacement for the built-in `read_file` tool. Controls the maximum file size the agent can read and the default approval policy.

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `max_file_size` | int | `1048576` | Maximum file size in bytes to read (1 MiB). |
| `default_policy` | string | `"allow"` | Default approval level: `"allow"`, `"ask"`, or `"deny"`. |

<Tabs>
  <Tab title="Default (zero config)">
    ```yaml
    modules:
      tool.file_read: {}
    ```
  </Tab>
  <Tab title="Custom">
    ```yaml
    modules:
      tool.file_read:
        max_file_size: 5242880
        default_policy: "allow"
    ```
  </Tab>
</Tabs>

## tool.file_write

Configurable replacement for the built-in `write_file` tool. Controls content size limits, automatic directory creation, and approval policy.

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `max_file_size` | int | `1048576` | Maximum content size in bytes to write (1 MiB). |
| `create_dirs` | bool | `true` | Automatically create parent directories on write. |
| `default_policy` | string | `"allow"` | Default approval level: `"allow"`, `"ask"`, or `"deny"`. |

<Tabs>
  <Tab title="Default (zero config)">
    ```yaml
    modules:
      tool.file_write: {}
    ```
  </Tab>
  <Tab title="Custom">
    ```yaml
    modules:
      tool.file_write:
        max_file_size: 2097152
        create_dirs: false
        default_policy: "ask"
    ```
  </Tab>
</Tabs>
